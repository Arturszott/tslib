import { subtract } from './subtract';

/**
 *
 * @param a -
 * @param b -
 *
 * @public
 */
function sum(a: number, b: number): number {
	const result = a + b;

	return result;
}

export { subtract, sum };
